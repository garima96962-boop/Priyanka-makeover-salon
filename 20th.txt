Good. Now let me create the remaining screens: services, gallery, book, contact, and the stack screens (about, bridal, testimonials, offers, blog, faq, admin):
Action: file_editor create /app/frontend/app/(tabs)/services.tsx --file-text "import React, { useMemo, useState } from 'react';
import { View, Text, StyleSheet, ScrollView, Pressable, FlatList } from 'react-native';
import { Image } from 'expo-image';
import { SafeAreaView } from 'react-native-safe-area-context';
import { Feather } from '@expo/vector-icons';
import { useRouter } from 'expo-router';
import { colors, spacing, radius, shadow, fonts } from '@/src/theme';
import { SERVICES, CATEGORIES } from '@/src/data';

export default function Services() {
  const [cat, setCat] = useState<string>('All');
  const router = useRouter();
  const list = useMemo(() => (cat === 'All' ? SERVICES : SERVICES.filter((s) => s.category === cat)), [cat]);

  return (
    <View style={{ flex: 1, backgroundColor: colors.surface }}>
      <SafeAreaView edges={['top']} style={styles.header}>
        <Text style={styles.eyebrow}>SALON MENU</Text>
        <Text style={styles.title}>Services</Text>
        <ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={styles.chipRow}>
          {CATEGORIES.map((c) => {
            const active = cat === c;
            return (
              <Pressable
                key={c}
                onPress={() => setCat(c)}
                style={[styles.chip, active && styles.chipActive]}
                testID={`chip-${c}`}
              >
                <Text style={[styles.chipText, active && styles.chipTextActive]}>{c}</Text>
              </Pressable>
            );
          })}
        </ScrollView>
      </SafeAreaView>

      <FlatList
        data={list}
        keyExtractor={(i) => i.id}
        contentContainerStyle={{ paddingHorizontal: spacing.xl, paddingBottom: 120, paddingTop: spacing.md, gap: spacing.md }}
        renderItem={({ item }) => (
          <View style={styles.card} testID={`service-${item.id}`}>
            <Image source={item.image} style={styles.cardImg} contentFit=\"cover\" />
            <View style={styles.cardBody}>
              <Text style={styles.cardCat}>{item.category.toUpperCase()}</Text>
              <Text style={styles.cardTitle}>{item.name}</Text>
              <Text style={styles.cardDesc} numberOfLines={2}>{item.desc}</Text>
              <View style={styles.cardFoot}>
                <View>
                  <Text style={styles.price}>{item.price}</Text>
                  <Text style={styles.duration}>{item.duration}</Text>
                </View>
                <Pressable
                  style={styles.bookBtn}
                  onPress={() => router.push({ pathname: '/(tabs)/book', params: { service: item.name } })}
                  testID={`book-${item.id}`}
                >
                  <Text style={styles.bookBtnText}>Book</Text>
                  <Feather name=\"arrow-right\" size={14} color={colors.charcoal} />
                </Pressable>
              </View>
            </View>
          </View>
        )}
      />
    </View>
  );
}

const styles = StyleSheet.create({
  header: { paddingHorizontal: spacing.xl, paddingBottom: spacing.md, backgroundColor: colors.surface, borderBottomWidth: StyleSheet.hairlineWidth, borderBottomColor: colors.border },
  eyebrow: { color: colors.brandStrong, letterSpacing: 2, fontSize: 10, fontFamily: fonts.sans, marginTop: spacing.md },
  title: { fontFamily: fonts.serif, fontSize: 32, color: colors.charcoal, fontWeight: '500', marginTop: 4, marginBottom: spacing.md },
  chipRow: { gap: 8, paddingRight: spacing.xl, paddingVertical: 4 },
  chip: { paddingHorizontal: 16, height: 36, borderRadius: radius.pill, borderWidth: 1, borderColor: colors.borderStrong, alignItems: 'center', justifyContent: 'center', flexShrink: 0, backgroundColor: colors.surface },
  chipActive: { backgroundColor: colors.charcoal, borderColor: colors.charcoal },
  chipText: { fontFamily: fonts.sans, fontSize: 12, color: colors.charcoal },
  chipTextActive: { color: colors.pearl },

  card: { flexDirection: 'row', backgroundColor: colors.pearl, borderRadius: radius.lg, overflow: 'hidden', borderWidth: 1, borderColor: colors.border },
  cardImg: { width: 110, height: '100%' },
  cardBody: { flex: 1, padding: spacing.md, gap: 4 },
  cardCat: { fontFamily: fonts.sans, fontSize: 9, letterSpacing: 1.5, color: colors.brandStrong },
  cardTitle: { fontFamily: fonts.serif, fontSize: 16, color: colors.charcoal, fontWeight: '500' },
  cardDesc: { fontFamily: fonts.sans, fontSize: 11, color: colors.muted, lineHeight: 16 },
  cardFoot: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'flex-end', marginTop: spacing.sm },
  price: { fontFamily: fonts.serif, fontSize: 15, color: colors.charcoal, fontWeight: '500' },
  duration: { fontFamily: fonts.sans, fontSize: 10, color: colors.muted, marginTop: 2 },
  bookBtn: { flexDirection: 'row', alignItems: 'center', gap: 4, backgroundColor: colors.brand, paddingVertical: 8, paddingHorizontal: 14, borderRadius: radius.pill },
  bookBtnText: { fontFamily: fonts.sans, fontSize: 12, color: colors.charcoal, fontWeight: '500' },
});
"
Observation: Create successful: /app/frontend/app/(tabs)/services.tsx