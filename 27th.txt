
Action: file_editor create /app/frontend/app/blog/index.tsx --file-text "import React from 'react';
import { View, Text, StyleSheet, ScrollView, Pressable } from 'react-native';
import { Image } from 'expo-image';
import { SafeAreaView } from 'react-native-safe-area-context';
import { Feather } from '@expo/vector-icons';
import { useRouter } from 'expo-router';
import { colors, spacing, radius, fonts } from '@/src/theme';
import { BLOG } from '@/src/data';

export default function Blog() {
  const router = useRouter();
  return (
    <View style={{ flex: 1, backgroundColor: colors.surface }}>
      <SafeAreaView edges={['top']} style={styles.head}>
        <Pressable onPress={() => router.back()} style={styles.back}><Feather name=\"arrow-left\" size={20} color={colors.charcoal} /></Pressable>
        <Text style={styles.eyebrow}>JOURNAL</Text>
        <Text style={styles.title}>Beauty Tips & Trends</Text>
      </SafeAreaView>
      <ScrollView contentContainerStyle={{ padding: spacing.xl, paddingBottom: 60, gap: spacing.lg }} showsVerticalScrollIndicator={false}>
        {BLOG.map((b) => (
          <Pressable key={b.id} style={styles.card} onPress={() => router.push(`/blog/${b.id}`)} testID={`blog-item-${b.id}`}>
            <Image source={b.cover} style={styles.img} contentFit=\"cover\" />
            <View style={{ padding: spacing.lg }}>
              <Text style={styles.cat}>{b.category.toUpperCase()} · {b.date}</Text>
              <Text style={styles.itemTitle}>{b.title}</Text>
              <Text style={styles.excerpt}>{b.excerpt}</Text>
              <View style={styles.readMore}>
                <Text style={styles.readText}>Read more</Text>
                <Feather name=\"arrow-right\" size={14} color={colors.charcoal} />
              </View>
            </View>
          </Pressable>
        ))}
      </ScrollView>
    </View>
  );
}

const styles = StyleSheet.create({
  head: { paddingHorizontal: spacing.xl, paddingBottom: spacing.md, borderBottomWidth: StyleSheet.hairlineWidth, borderBottomColor: colors.border },
  back: { width: 40, height: 40, borderRadius: radius.pill, borderWidth: 1, borderColor: colors.borderStrong, alignItems: 'center', justifyContent: 'center', marginTop: spacing.sm },
  eyebrow: { color: colors.brandStrong, letterSpacing: 2, fontSize: 10, fontFamily: fonts.sans, marginTop: spacing.md },
  title: { fontFamily: fonts.serif, fontSize: 30, color: colors.charcoal, fontWeight: '500', marginTop: 4 },
  card: { borderRadius: radius.lg, overflow: 'hidden', backgroundColor: colors.pearl, borderWidth: 1, borderColor: colors.border },
  img: { width: '100%', height: 200 },
  cat: { fontFamily: fonts.sans, fontSize: 10, letterSpacing: 1.5, color: colors.brandStrong },
  itemTitle: { fontFamily: fonts.serif, fontSize: 20, color: colors.charcoal, marginTop: 6, fontWeight: '500', lineHeight: 26 },
  excerpt: { fontFamily: fonts.sans, fontSize: 13, color: colors.muted, marginTop: 6, lineHeight: 20 },
  readMore: { flexDirection: 'row', alignItems: 'center', gap: 4, marginTop: spacing.md },
  readText: { fontFamily: fonts.sans, color: colors.charcoal, fontSize: 12, fontWeight: '500', textDecorationLine: 'underline' },
});
"
Observation: Create successful: /app/frontend/app/blog/index.tsx